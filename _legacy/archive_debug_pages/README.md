# Archived Debug Pages

These pages were removed from active website render/navigation on 2026-03-11.

Archived files:
- `datas.qmd`
- `clusters.qmd`
- `planta_debug.qmd`
- `relacoes_debug.qmd`

Reason:
- They are debug/auxiliary pages and should not appear in the public navbar (`Mais` menu removed).

If one of these analyses needs to return to production, move the file back to repository root and re-add it in `_quarto.yml` (`project.render` and optional `website.navbar`).
